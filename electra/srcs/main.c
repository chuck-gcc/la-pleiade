#include "vm_managment/vm.h"
#include "offer_managment/offer.h"
#include <libxml/parser.h>

int main(void)
{
    get_offer_list();
    // virConnectPtr co;

    // co = host_connection();
    // if(!co)
    //     return(1);
    // list_actif_domain(co);
    // //create_vm(co);

    // get_ressources(co);
    // virConnectClose(co);
    return(0);
}